local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()

local Window = Fluent:CreateWindow({
    Title = "Ohio Powers",
    SubTitle = "by veddetsiwt",
    TabWidth = 160,
    Size = UDim2.fromOffset(580, 460),
    Acrylic = true,
    Theme = "Darker",
    MinimizeKey = Enum.KeyCode.LeftControl
})

local Tabs = {
    Main = Window:AddTab({ Title = "Main", Icon = "box" }),
    Powers = Window:AddTab({ Title = "Powers", Icon = "rocket" }),
    Autofarm = Window:AddTab({ Title = "Autofarm", Icon = "gem" }),
    Settings = Window:AddTab({ Title = "Settings", Icon = "settings" })
}

local Options = Fluent.Options
local AutofarmActive = false

do
    local AutofarmToggle = Tabs.Autofarm:AddToggle("AutofarmToggle", {Title = "Autofarm Active", Default = false })
    AutofarmToggle:OnChanged(function()
        AutofarmActive = not AutofarmActive
        if Options.MyToggle.Value then
            Fluent:Notify({
                Title = "Autofarm",
                Content = "Enabled Autofarm",
                Duration = 6
            })
        else
            Fluent:Notify({
                Title = "Autofarm",
                Content = "Disabled Autofarm",
                Duration = 6
            })
        end
    end)


    Tabs.Main:AddButton({
        Title = "Unload GUI",
        Description = "Very important button",
        Callback = function()
            Window:Dialog({
                Title = "Confirmation",
                Content = "Are you sure you want to unload the GUI?",
                Buttons = {
                    {
                        Title = "Confirm",
                        Callback = function()
                            Window:Destroy()
                            print("Confirmed the dialog.")
                        end
                    },
                    {
                        Title = "Cancel",
                        Callback = function()
                            print("Cancelled the dialog.")
                        end
                    }
                }
            })
        end
    })
end
